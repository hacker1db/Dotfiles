#!/bin/bash
#Generated by JetBrains Toolbox 2.6.1.40902 at 2025-04-23T01:28:23

declare -a intellij_args=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    intellij_args+=("\"$o\"")
  else
    intellij_args+=("$o")
  fi
done

open -na "/Users/David.Walters@AlaskaAir.com/Applications/Writerside.app/Contents/MacOS/writerside" $wait --args "${intellij_args[@]}"