#!/bin/bash
#Generated by JetBrains Toolbox 1.26.4.13374 at 2022-10-28T23:48:18.254311

declare -a ideargs=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    ideargs+=("\"$o\"")
  else
    ideargs+=("$o")
  fi
done

open -na "/Users/hacker1db/Library/Application Support/JetBrains/Toolbox/apps/WebStorm/ch-0/223.7255.1/WebStorm 2022.3 EAP.app/Contents/MacOS/webstorm" $wait --args "${ideargs[@]}"