---
description: Convert an Obsidian markdown blog post to hacker1db.dev `.svx`.
---

# Markdown To SVX

Use the `md-to-svx` skill with `$ARGUMENTS`.

Preserve the skill workflow:
- Read the blog voice/config reference before converting.
- Locate the Obsidian source post and active SvelteKit worktree.
- Transform frontmatter, Obsidian syntax, embeds, callouts, and category/slug mapping.
- Write the `.svx` output and summarize transformations plus any manual follow-up.
- In publish mode, set `draft: false` and show the blog repo diff stat.
